<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_alignParentTop="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:minHeight="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:contentInsetStart="14dp"
            app:contentInsetLeft="14dp"
            android:elevation="4dp"
            android:theme="?attr/actionBarStyle" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tab_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:elevation="4dp"
            android:layout_gravity="top"
            android:background="?attr/colorPrimary"
            app:tabMode="scrollable"
            app:tabPaddingStart="8dp"
            app:tabPaddingEnd="8dp"
            app:tabBackground="?attr/colorPrimary"
            app:tabIndicatorColor="@color/white"
            app:tabTextColor="@color/gray10"
            app:tabSelectedTextColor="@color/white" />

    </com.google.android.material.appbar.AppBarLayout>

    <!--
        BMChat 2.49.80 (Phase 3): Telegram-style mini-player pinned to
        the bottom of the Shared Media browser. Reuses the same view as
        the one in ConversationListActivity / ConversationActivity, so a
        single track in the background is reachable from every screen.
    -->
    <org.thoughtcrime.securesms.components.audioplay.BMChatMiniPlayerView
        android:id="@+id/bmchat_miniplayer"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/pager"
        android:layout_below="@id/appbar"
        android:layout_above="@id/bmchat_miniplayer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</RelativeLayout>
