<?xml version="1.0" encoding="utf-8"?>
<!--
  One command row in the editor. The layout intentionally mirrors a
  classic name/value pair so the user can think of it as "if /<name>
  arrives, reply with this".
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginBottom="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="2dp"
            android:textColor="?android:attr/textColorPrimary"
            android:textSize="14sp"
            android:text="/" />

        <EditText
            android:id="@+id/email_bot_command_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:inputType="text"
            android:singleLine="true"
            android:hint="@string/bmchat_email_bot_command_name_hint" />

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/email_bot_command_delete"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/bmchat_email_bot_remove_command"
            android:src="@drawable/ic_close_white_24dp"
            android:tint="?android:attr/textColorSecondary" />
    </LinearLayout>

    <EditText
        android:id="@+id/email_bot_command_reply"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:maxLines="6"
        android:hint="@string/bmchat_email_bot_command_reply_hint" />
</LinearLayout>
