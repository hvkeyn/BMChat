<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_layout"
    android:background="?attr/input_panel_bg_color"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <include layout="@layout/status_bar_background" />

    <org.thoughtcrime.securesms.components.ScaleStableImageView
        android:id="@+id/conversation_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:ignore="ContentDescription"
        android:scaleType="centerCrop" />

    <org.thoughtcrime.securesms.components.InputAwareLayout
        android:id="@+id/layout_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:id="@+id/conversation_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="bottom"
            android:clipToPadding="false"
            android:clipChildren="false">

            <FrameLayout
                android:id="@+id/fragment_content"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <org.thoughtcrime.securesms.components.audioplay.BMChatMiniPlayerView
                android:id="@+id/bmchat_miniplayer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <ViewStub
                android:id="@+id/attachment_editor_stub"
                android:inflatedId="@+id/attachment_editor"
                android:layout="@layout/conversation_activity_attachment_editor_stub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <org.thoughtcrime.securesms.messagerequests.MessageRequestsBottomView
                android:id="@+id/conversation_activity_message_request_bottom_bar"
                android:background="?android:attr/windowBackground"
                android:visibility="gone"
                android:clickable="true"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingLeft="16dp"
                android:paddingStart="16dp"
                android:paddingRight="16dp"
                android:paddingEnd="16dp"
                android:focusable="true" />

            <include layout="@layout/conversation_input_panel" />

            <FrameLayout
                android:id="@+id/emoji_picker_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

        </LinearLayout>
    </org.thoughtcrime.securesms.components.InputAwareLayout>

    <!-- BMChat 2.49.83 (Phase 5C): Telegram-style round video note bubble that appears above the
         input panel while the user holds the recorder button in video mode. -->
    <FrameLayout
        android:id="@+id/bmchat_video_note_overlay"
        android:layout_width="172dp"
        android:layout_height="172dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginEnd="14dp"
        android:layout_marginRight="14dp"
        android:layout_marginBottom="64dp"
        android:visibility="gone"
        android:elevation="6dp"
        android:background="@drawable/bmchat_video_note_ring"
        android:clipChildren="true"
        android:clipToPadding="false">

        <androidx.camera.view.PreviewView
            android:id="@+id/bmchat_video_note_overlay_preview"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_gravity="center" />

        <TextView
            android:id="@+id/bmchat_video_note_overlay_timer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="6dp"
            android:background="#88000000"
            android:paddingStart="8dp"
            android:paddingLeft="8dp"
            android:paddingEnd="8dp"
            android:paddingRight="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:textColor="#ffffff"
            android:textSize="12sp"
            android:textStyle="bold"
            tools:text="0:08" />
    </FrameLayout>
</RelativeLayout>
